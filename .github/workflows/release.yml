name: release

on:
  push:
    tags:
      - "v*.*.*"
  workflow_dispatch:
    inputs:
      dry_run:
        description: Build and verify without publishing
        type: boolean
        default: true

permissions:
  contents: read

jobs:
  build:
    name: reproducible build
    runs-on: ubuntu-latest
    permissions:
      contents: read
    outputs:
      rules_version: ${{ steps.build.outputs.rules_version }}
      stability: ${{ steps.build.outputs.stability }}
      artifact_sha256: ${{ steps.build.outputs.artifact_sha256 }}
      conformance_sha256: ${{ steps.build.outputs.conformance_sha256 }}
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          fetch-depth: 0
      - uses: actions/setup-go@b7ad1dad31e06c5925ef5d2fc7ad053ef454303e # v7.0.0
        with:
          go-version-file: go.mod
          cache: true

      - name: Quality gates
        run: |
          go test -race ./...
          go run ./cmd/entidc verify
          make cover

      - name: The tag must name the rules version
        # README: "One release is one rulesVersion and one immutable Git tag
        # v<rulesVersion>". Nothing enforced it, and practice drifted: v0.1.0
        # carried rules 2026.08.32 and v0.1.1 carried 2026.08.33. The tag and the
        # version were two independent axes, so nothing stopped two tags from
        # delivering one rulesVersion -- the exact collision rules.lock cannot
        # represent, since it identifies a bundle by that version and its digest.
        # The tag ruleset already makes v* immutable; this makes that immutability
        # protect the invariant the README states.
        if: startsWith(github.ref, 'refs/tags/')
        run: |
          set -euo pipefail
          expected="v$(cat RULES_VERSION)"
          if [ "${GITHUB_REF_NAME}" != "${expected}" ]; then
            echo "::error::tag ${GITHUB_REF_NAME} does not name the rules version; expected ${expected}"
            exit 1
          fi
          echo "tag ${GITHUB_REF_NAME} names rules $(cat RULES_VERSION)"

      - name: Build twice in two distinct temporary directories
        id: build
        run: |
          set -euo pipefail
          SOURCE_DATE_EPOCH="$(git log -1 --pretty=%ct)"
          export SOURCE_DATE_EPOCH
          commit="$(git rev-parse HEAD)"
          for suffix in one two-with-a-longer-name; do
            go run ./cmd/entidc compile \
              --out "${RUNNER_TEMP}/dist-${suffix}" \
              --release --source-commit "$commit"
          done
          diff -r "${RUNNER_TEMP}/dist-one" "${RUNNER_TEMP}/dist-two-with-a-longer-name"
          cp -a "${RUNNER_TEMP}/dist-one" dist
          version="$(cat RULES_VERSION)"
          manifest="dist/entid-manifest-${version}.json"
          test "$(jq -r .reproducible "$manifest")" = "true"
          stability="$(jq -r .stability "$manifest")"
          echo "rules_version=${version}" >> "$GITHUB_OUTPUT"
          echo "stability=${stability}" >> "$GITHUB_OUTPUT"
          echo "artifact_sha256=$(jq -r .artifactSha256 "$manifest")" >> "$GITHUB_OUTPUT"
          echo "conformance_sha256=$(jq -r .conformanceSha256 "$manifest")" >> "$GITHUB_OUTPUT"

      - name: Verify SHA256SUMS
        working-directory: dist
        run: sha256sum --check --strict SHA256SUMS

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: release-artifacts
          path: dist/
          if-no-files-found: error

  publish:
    name: attest and publish
    needs: build
    runs-on: ubuntu-latest
    environment: release
    permissions:
      contents: write
      id-token: write
      attestations: write
    steps:
      - uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: release-artifacts
          path: dist

      - name: Verify SHA256SUMS again before publishing
        working-directory: dist
        run: sha256sum --check --strict SHA256SUMS

      - name: Build the provenance attestation
        id: attest
        uses: actions/attest-build-provenance@4d101475d8b20a2381f78447822ac1eab6504dd8 # v4.2.2
        with:
          subject-path: |
            dist/entid-rules-${{ needs.build.outputs.rules_version }}.binpb
            dist/entid-conformance-${{ needs.build.outputs.rules_version }}.binpb
            dist/entid-conformance-${{ needs.build.outputs.rules_version }}.jsonl.gz
            dist/entid-manifest-${{ needs.build.outputs.rules_version }}.json
            dist/rules.proto
            dist/conformance.proto
            dist/testee.proto
            dist/ir.md
            dist/features.md
            dist/spec.md
            dist/engine.md
            dist/engine-go.md
            dist/engine-swift.md
            dist/engine-kotlin.md
            dist/engine-typescript.md
            dist/provenance-go.md
            dist/provenance-swift.md
            dist/provenance-kotlin.md
            dist/provenance-typescript.md
            dist/SHA256SUMS
            dist/SBOM.spdx.json

      - name: Collect the attestation bundle
        run: cp "${{ steps.attest.outputs.bundle-path }}" dist/provenance.intoto.jsonl

      - name: Publish the release
        if: startsWith(github.ref, 'refs/tags/') && inputs.dry_run != true
        env:
          GH_TOKEN: ${{ github.token }}
        run: |
          set -euo pipefail
          # A pre-release stays out of the releases/latest endpoint, so a
          # consumer or a downstream script never picks it up by accident.
          prerelease=""
          if [ "${{ needs.build.outputs.stability }}" != "stable" ]; then
            prerelease="--prerelease"
          fi
          gh release create "${GITHUB_REF_NAME}" \
            --title "Rules ${{ needs.build.outputs.rules_version }} (${{ needs.build.outputs.stability }})" \
            --notes-file docs/generated/coverage.md \
            ${prerelease} \
            dist/*
